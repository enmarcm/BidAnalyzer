const Loader = () => {
  return (
    <div className="absolute z-50 w-full h-full backdrop-blur-md bg-transparent flex items-center justify-center transition-none">
      <span className="loader transition-none"></span>
    </div>
  );
};

export default Loader;